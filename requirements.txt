# Core RL
stable-baselines3==2.8.0
gymnasium==1.2.3
torch==2.12.0
numpy==2.4.6

# Dashboard
Flask==3.1.3
flask-cors==6.0.5

# Production server (SSE needs single worker + gevent)
gunicorn==23.0.0
gevent==24.11.1

# Visualization (pygame omitted — desktop-only, not needed for web server)
matplotlib==3.11.0
pandas==3.0.3
pillow==12.2.0

# Experiment tracking
tensorboard==2.20.0
mlflow==2.15.1

# Hyperparameter optimization
optuna==3.6.1

# LLM mission planner (Groq)
groq==1.4.0

# Utilities
colorama==0.4.6
